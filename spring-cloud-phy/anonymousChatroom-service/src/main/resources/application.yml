spring:
  application:
    name: anonymousChatroom-service
  config:
    import: nacos:anonymousChatroom-service-dev.yml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml