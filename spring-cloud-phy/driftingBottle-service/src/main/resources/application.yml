spring:
  application:
    name: driftingBottle-service
  config:
    import: nacos:driftingBottle-service-dev.yml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/driftbottle_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

    username: root
    password: sr200416
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
logging:
  level:
    cn.itcast: debug
  pattern:
    datefotmat: MM-dd HH:mm:ss:$$$